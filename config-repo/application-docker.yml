eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh,env

logging:
  logstash:
    host: logstash
    port: 5044